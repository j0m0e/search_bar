<% if session[:zip_code] %>
  Welcome, <%= User.find(session[:current_user_id]).username %>
  <%= link_to "Log Out", sessions_path, method: :delete %>
<% else %>
	<%= link_to "Log In", login_path %> |
	<%= link_to "Sign Up", new_user_path %>
<% end %>

<!-- one of the two arguments in the search api method -->